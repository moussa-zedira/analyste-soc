# API target
API_BASE_URL=http://localhost:8000

# Security (must match API's API_KEY)
API_KEY=change-me

# Modes: normal | bruteforce | continuous
AGENT_MODE=normal

# Delay between events in continuous mode (milliseconds)
AGENT_INTERVAL_MS=1000
